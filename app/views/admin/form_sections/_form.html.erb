<%= form_for([:admin, @form_section], html: { role: "form", class: "form-horizontal"}) do |f| %>

  <fieldset>
    <legend>Dados Básicos</legend>
    <div class="form-group">
      <%= f.label :name, "Nome", class: "col-sm-2 control-label" %>
      <div class="col-sm-10">
        <%= f.text_field :name, class: "form-control", placeholder: "nome" %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :order, "Ordem", class: "col-sm-2 control-label" %>
      <div class="col-sm-10">
        <%= f.text_field :order, class: "form-control", placeholder: "ordem" %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :form_section_id, "Dependecia", class: "col-sm-2 control-label" %>
      <div class="col-sm-10">
        <%= f.select :form_section_id, FormSection.all.collect{|a| [ a.name, a.id ] }, { include_blank: true }, class: "form-control", placeholder: "nome" %>
      </div>
    </div>

  </fieldset>


  <fieldset>
    <legend></legend>
    <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
        <button type="submit" class="btn btn-success">Salvar</button> ou 
        <a href="<%= admin_form_section_path( @form_section )%>">cancelar</a>
      </div>
    </div>
  </fieldset>

<% end %>
