<%= form_for([ :admin, @survey, @section], html: { role: "form", class: "form-horizontal"} ) do |f| %>

  <fieldset>
    <legend>Dados Básicos</legend>

    <div class="form-group">
      <%= f.label :name, "Nome", class: "col-sm-2 control-label" %>
      <div class="col-sm-10">
        <%= f.text_field :name, class: "form-control", placeholder: "nome" %>
      </div>
    </div>
  
    <div class="form-group">
      <%= f.label :help_block, "Texto de ajuda", class: "col-sm-2 control-label" %>
      <div class="col-sm-10">
        <%= f.text_field :help_block, class: "form-control", placeholder: "nome" %>
      </div>
    </div>

  </fieldset>


  <fieldset>
    <legend></legend>
    <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
        <button type="submit" class="btn btn-success">Salvar</button> ou 
        <button type="submit" data-save-add-other class="btn btn-success">Salvar e adicionar outro</button> ou 
        <a href="<%= admin_survey_sections_path( @survey ) %>">cancelar</a>
      </div>
    </div>
  </fieldset>

<% end %>
