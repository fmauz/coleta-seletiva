<%content_for :title do%>
<h1><%= I18n.t("resource.#{@survey.class.name.downcase}")%></h1>
<%end%>

<%= flash_notice_render %>

<p>
  <strong>Nome:</strong> <%= @survey.name %>
</p>

<div class="clone_questions">
  <h4>Copiar perguntas e respostas de outro formulário para este.</h4>

  <%= form_tag admin_survey_clone_path( @survey.id ) do -%>
    <p>
      <strong>Base:</strong>
      <%= select_tag "survey_base", options_from_collection_for_select(Survey.all, :id, :name) %>
    </p>

    <p>
      <button type="submit" class="btn btn-default">Clonar</button>
    </p>
  <% end -%>
</div>



<%= link_to 'Editar', edit_admin_survey_path(@survey), class: "btn btn-lg btn-warning" %> ou
<%= link_to 'voltar', admin_surveys_path %>
